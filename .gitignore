# Secrets & local config
# Ignore ALL env variants (.env.1pixel, .env.feDUP, wallet keys, etc.);
# keep only the committed template .env.example (no secrets).
.env
.env.*
!.env.example
config/config.yaml
config/markets.yaml
config/*.local.yaml

# Data & reports (local, per-machine)
data/
*.sqlite
*.sqlite3
*.db
reports/
*.xlsx

# Keys
*.pem
secrets/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.ruff_cache/
build/
dist/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
